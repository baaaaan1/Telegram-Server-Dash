name: CI

on:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
      - name: Run ruff linter
        run: ruff check .
      - name: Run ruff format check
        run: ruff format --check .

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
      - name: Run tests
        run: |
          pytest -v --tb=short
      - name: Upload coverage
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          fail_ci_if_error: false

  deployment:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate shell entrypoints
        run: |
          bash -n setup.sh
          bash -n deploy/lib/ui.sh
          shellcheck -x setup.sh deploy/lib/ui.sh
      - name: Smoke test the CLI surface
        run: |
          ./setup.sh --version
          ./setup.sh --help > /dev/null
          if ./setup.sh install > /dev/null 2>&1; then
            echo "install must refuse to run without root" >&2
            exit 1
          fi
          if ./setup.sh frobnicate > /dev/null 2>&1; then
            echo "unknown commands must exit non-zero" >&2
            exit 1
          fi
          ./setup.sh --plain --no-banner status > /dev/null 2>&1 || true
      - name: Verify systemd unit
        run: |
          sudo useradd --system --home-dir /nonexistent --no-create-home --shell /usr/sbin/nologin --user-group tsd
          sudo install -d /opt/telegram-server-dash/current/.venv/bin
          sudo install -d /etc/telegram-server-dash /var/lib/telegram-server-dash
          sudo install -m 0755 /bin/true /opt/telegram-server-dash/current/.venv/bin/python
          sudo install -m 0600 /dev/null /etc/telegram-server-dash/telegram-server-dash.env
          sudo systemd-analyze verify deploy/telegram-server-dash.service
